WEBAPP_CALLBACK_URL=http://127.0.0.1:3000/api/submissions/callback
SHARED_SECRET=a-very-long-and-random-shared-secret
ENABLE_GRADIO=false
GRADIO_PATH=/gradio
SANDBOX_BACKEND=CHROOT  # 可选: CHROOT 或 DOCKER
# 当 SANDBOX_BACKEND=DOCKER 时的参数:
DOCKER_IMAGE=swr.cn-north-4.myhuaweicloud.com/ddn-k8s/docker.io/library/python:3.12-slim
# 如果 EvaluateApp 以容器运行，且希望评测容器与服务使用相同镜像，可设置：
# DOCKER_IMAGE=self
DOCKER_PULL=false
DOCKER_MEMORY=2g
DOCKER_CPUS=1.0
DOCKER_NETWORK_MODE=none
# DOCKER_USER=65534:65534  # 可选，指定容器内用户（例如 nobody）
